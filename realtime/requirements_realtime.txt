# =============================================================================
# Animal Vocalization Analysis Framework - Real-Time Dependencies
# =============================================================================
# Additional dependencies for real-time audio interaction and low-latency processing
# =============================================================================
# Install base requirements.txt first, then add these for real-time features
# =============================================================================

# =============================================================================
# REAL-TIME AUDIO HARDWARE INTERFACE
# =============================================================================
# Low-latency audio I/O for microphone input and speaker output
# NOTE: These are optional - the framework primarily uses soundfile for offline processing

# Primary audio interface (requires system PortAudio library)
# Install system libraries first:
#   Linux: sudo apt-get install portaudio19-dev
#   macOS: brew install portaudio
#   Windows: Download from https://www.portaudio.com/
# sounddevice>=0.5.0

# Alternative audio interface (good fallback)
# pyaudio>=0.2.14

# =============================================================================
# LOW-LATENCY AUDIO PROCESSING
# =============================================================================
# Optimized for real-time performance

# Voice activity detection for noise filtering
# webrtcvad>=2.0.10

# =============================================================================
# CONFIGURATION AND LOGGING
# =============================================================================
# For real-time system configuration and debugging

# PyYAML>=6.0          # Configuration file management
# colorlog>=6.7.0      # Colored logging for real-time feedback

# =============================================================================
# INSTALLATION INSTRUCTIONS
# =============================================================================
#
# Step 1: Install system audio libraries (if using real-time audio I/O)
#   Linux: sudo apt-get install portaudio19-dev libasound2-dev
#   macOS: brew install portaudio
#   Windows: Download and install PortAudio from https://www.portaudio.com/
#
# Step 2: Install base dependencies
#   pip install -r requirements.txt
#
# Step 3: Install real-time specific dependencies (optional)
#   pip install -r requirements_realtime.txt
#
# Note: The framework works primarily with offline audio processing (soundfile).
#       Real-time audio I/O (sounddevice/pyaudio) is optional for live interaction.
# =============================================================================
