# =============================================================================
# Animal Vocalization Analysis Framework - Core Dependencies
# =============================================================================
# Minimal required dependencies for the framework
# =============================================================================

# Core scientific computing (required)
numpy>=2.0.0
scipy>=1.10.0

# Audio processing (required)
librosa>=0.10.0
soundfile>=0.12.0

# Machine learning (required for cognitive intelligence modules)
torch>=2.0.0
scikit-learn>=1.3.0

# Data handling (required for data import)
pandas>=2.0.0

# Progress tracking (required for CLI tools)
tqdm>=4.65.0

# System monitoring (minimal usage in task_management)
psutil>=5.9.0

# Visualization (required for analysis and plotting)
matplotlib>=3.7.0
seaborn>=0.12.0

# Computer vision (required for postprocessing visual recording)
opencv-python>=0.24.0

# Async networking (required for unified_database)
aiohttp>=3.9.0

# ZeroMQ messaging (required for python_agent heartbeat)
pyzmq>=25.0.0

# Parallel processing (built-in, but joblib is still useful)
joblib>=1.1.0
